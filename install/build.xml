<project name="Lab Tools Library for Salesforce" default="CheckLabTools" basedir="." xmlns:sf="antlib:com.salesforce">

    <property file="build.properties"/>
    <property environment="env"/>

    <!-- Deploy the unpackaged set of metadata retrieved with retrieveUnpackaged -->
    <target name="DeployLabTools" description="Deploys the Lab Tools Library for Salesforce to your organization">
      <echo message="Deploying the Lab Tools Library for Salesforce..."/>
      <sf:deploy username="${sf.username}" password="${sf.password}" serverurl="${sf.serverurl}" deployRoot="../src"/>
    </target>

    <!-- Shows check only; never actually saves to the server -->
    <target name="CheckLabTools" description="Checks that the Lab Tools Library can be deployed without making any changes">
      <echo message="Run 'ant DeployLabTools' to deploy this library to your organization."/>
      <echo message="Testing the deployment of the Lab Tools Library for Salesforce without making any changes..."/>
      <sf:deploy username="${sf.username}" password="${sf.password}" serverurl="${sf.serverurl}" deployRoot="../src" checkOnly="true"/>
    </target>

</project>
